import { useState } from 'react'
import './App.css'

function App() {
  const minimumScore = 20000;
  const [currentScore, setCurrentScore] = useState(0);

  return (
    <main>
      <h1>hello</h1>
    </main>
  )
}

export default App
